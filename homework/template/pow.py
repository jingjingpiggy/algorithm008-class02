
def myPow(x, n):
    if problem is None:
        print_result
        return

    data = prepare_data(x)
    subp = split_problem(x, data)

    subr1 = myPow(subp[0], p1, ...)
    subr2 = myPow(subp[1], p1, ...)
    subr3 = myPow(subp[2], p1, ...)
